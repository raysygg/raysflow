# Enterprise Agent Studio 项目说明

## 项目架构

### 技术架构

- 后端：Java 17、Spring Boot 3.3.1、Spring Security、MyBatis-Plus、LangChain4j。
- 前端：Vue 3、Vue Router、Element Plus、Axios、Vite。
- 数据存储：MySQL 保存业务事实，Redis 承载缓存与实时能力，Qdrant 保存向量索引。
- 接口：REST API、WebSocket；前后端分离部署。
- 规格管理：OpenSpec 管理需求、设计、任务和变更状态。

### 后端分层

后端代码位于 `backend/src/main/java/com/acme/agentstudio`：

- `interfaces`：REST 控制器和外部接口适配层，只负责参数接收、身份上下文和响应转换。
- `application`：应用服务层，组织事务、权限、状态流转和跨领域业务流程。
- `domain`：领域模型、枚举、强类型契约和核心业务规则。
- `infrastructure`：MyBatis-Plus 实体与 Mapper、模型供应商、Qdrant、Redis及其他外部系统适配。
- `config`：安全、线程池、配置属性和框架装配。
- `common`：统一响应、异常和通用基础能力。

### 前端结构

前端代码位于 `frontend/src`：

- `views`：应用、知识、模型、评测、发布、运行、治理和商业中心等任务页面。
- `components`：通用页面组件和工作流节点配置组件。
- `api`：按业务领域封装后端接口及强类型数据转换。
- `router`：产品路由与权限入口。
- `layouts`：全局导航和页面布局。
- `constants`、`config`、`utils`：固定定义、运行配置和通用工具。

## 核心功能

- 统一应用生命周期：应用草稿、发布候选、评测、发布门禁、不可变版本、生产指针和回滚。
- 工作流编排：可视化节点、变量、条件、人工审批、工具调用和持久化执行。
- Agent Runtime：对话、任务执行、实时事件、暂停恢复、重试、重放和故障处理。
- 企业知识库：文档上传、解析、切块、索引 Generation、权限范围和多语言混合 RAG 检索。
- 模型中心：租户模型与平台模型路由、Embedding、Reranker、模型能力诊断和价格管理。
- 评测中心：版本化测试集、候选评测、质量指标、基线对比和发布门禁证据。
- 应用入口：对话、表单、API、Webhook 和定时任务入口，生产调用固定到活动 Release。
- 企业身份：OIDC、SAML、SCIM、MFA、管理员保护和紧急访问。
- SaaS 商业治理：套餐、订阅、权益准入、用量账本、成本中心、账期、账单和外部财务适配。
- 数据治理：保留策略、数据导出、删除审批、Legal Hold、跨存储执行和安全证据。
- 产品运营：应用运行监控、审计、安全诊断、采用漏斗、首次价值和留存指标。

## 开发规范

### 通用规范

- 修改范围必须与当前需求直接相关，不重构无关代码，不覆盖工作区已有修改。
- 优先采用简单、明确的实现；不为单次使用创建抽象，不增加未要求的扩展能力。
- 项目处于开发阶段，不保留旧接口、旧数据和旧业务逻辑的兼容分支。
- 基础数据需要调整时，提供明确的删除和插入 SQL，不在业务代码中长期保留迁移逻辑。
- 复杂流程添加简短中文注释，说明约束、原因和事务边界，避免逐行复述代码。
- 固定状态、阈值、角色名、字段名和限制集中到常量、枚举或配置类，禁止散落魔法值。

### 后端规范

- 跨层契约使用 `record`、DTO 和 `enum`，禁止新增无类型 `Map` 作为跨层接口。
- 查询和更新优先使用 MyBatis-Plus 的 `LambdaQueryWrapper`、`LambdaUpdateWrapper`。
- Lambda Wrapper 字段使用实体方法引用，例如 `Entity::getTenantId`。
- 所有业务查询必须包含租户边界；涉及应用、Release、Profile 或 Generation 时同时校验对应归属。
- 高风险业务使用清晰的事务边界，失败时不得留下半完成状态或返回虚假成功。
- 外部服务失败、超时或响应不完整时保存真实状态和错误分类，不生成默认成功结果。
- 安全、权限、租户隔离和合规规则优先于商业权益和降级策略。
- Secret 只保存引用，不写入业务表、日志、指标或 API 响应。
- 方法、类、字段、代码行需要添加中文注释，方法和类采用javadoc格式
- 异常信息、异常提示用中文友好信息
### 数据规范

- 数据库变更使用 `backend/sql` 下的顺序 SQL，不引入其他迁移框架。
- 新表必须包含必要的租户索引、业务关联索引、状态索引和幂等唯一约束。
- 用量、成本、审计和治理证据采用可追溯事实，不覆盖历史记录。
- 未知成本必须标记为 `UNKNOWN`，不得用零值伪装已计算成本。
- 指标、审计和调试信息不得保存凭证、完整查询、完整响应或文档正文。
- 部分失败必须保存各阶段结果并允许重试，不得显示为全部成功。

### 前端规范

- 页面以用户任务为中心，使用中文业务文案，不直接展示内部英文状态和技术标识。
- 身份、计费、数据治理、产品采用和平台运营使用独立任务页面，不复用同一运维页面代替产品功能。
- 页面状态以服务端事实为准，刷新后能够恢复运行、审批、评测和发布进度。
- 每个页面补齐加载、空数据、权限不足、后台处理中、部分失败和长错误状态。
- 保持企业 SaaS 的紧凑信息密度，避免超大字号、超大容器、无样式按钮和多层嵌套卡片。
- 使用项目已有图标库和组件体系；图标按钮提供中文悬浮提示。
- 桌面和移动布局不得出现文字溢出、控件重叠、内容遮挡或明显布局跳动。
- 用户可见之处，除logo等必须外，禁止使用英文，必须是中文，用户友好表示